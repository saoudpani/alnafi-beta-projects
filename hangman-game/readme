In order to run this program use python version 3 and run hangman.py.
Add requests libraray which is used to fetch words from datamuse API.
 TO INSTALL REQUESTS PACKAGE USE: 
 pip3 install requests 
 or visit https://pypi.org/project/requests
